---
description: Teach an agent something to remember (routes to the right agent's memory)
---

Aji is telling the studio something to remember: $ARGUMENTS

This writes to the **adjustable** layer only, never the constitution (`CLAUDE.md`, the agent definitions, the templates).

Steps:

1. Decide which agent the note is about:
   - Research, topics, angles, what performs → `memory/linda.md`
   - Voice, style, script craft, approved anecdotes, scheduling → `memory/torch.md`
   - What to check, review calibration, over/under-flagging → `memory/dan.md`
   - If it clearly affects more than one, also add it to `memory/cross-agent-lessons.md` tagged with who it affects.
2. Append it as a dated, one-line entry at the top of the right section. Keep Aji's meaning; tighten only for clarity.
3. If it contradicts something already logged, replace or annotate the old entry rather than duplicating.
4. Confirm back to Aji in one short line: what was logged and where.

If the note would actually change the constitution (e.g. "always use this new structure"), do not edit the constitution silently — say so and ask Aji to confirm that's a deliberate change to the stable layer.
