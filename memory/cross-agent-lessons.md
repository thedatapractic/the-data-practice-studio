# Cross-agent lessons

Lessons that affect **more than one agent**. When a correction in one agent's work carries a lesson for another (for example, Dan keeps flagging weak hooks, so Torch should open stronger; or Aji keeps reframing Linda's angle, so Torch should expect to sharpen the purpose), record it here **and** add it to each affected agent's own memory.

Each entry: `YYYY-MM-DD — [affects: Linda/Torch/Dan] — the lesson, as a rule.`

On its next task, each agent reads this file and copies any entry tagged for it into its own memory if it isn't already there, so the lesson sticks even in a fresh context.

## Lessons
- 2026-08-02 — [affects: Linda, Torch, Dan] **`domain-knowledge.md` now exists** and is part of the constitution: the field's verbs, disciplines, role definitions, quality dimensions, and the distinctions practitioners care about. Torch reads it before drafting and runs a verb check; Dan checks scripts against it as part of the domain-expertise item. Aji is the authority on its contents.
- 2026-08-02 — [affects: Torch, Dan] **Generic verbs are the tell.** "Run", "do", "handle" attached to data expose an outsider. Data is managed, governed, validated, profiled, cleansed, stewarded, retained, archived. Catch this in drafting, not review.
- 2026-08-02 — [affects: Linda, Torch, Dan] **Runtime rule relaxed:** target stays 7–8 minutes, but a script may run to a **maximum of 10 minutes** where the material genuinely earns it. The flexibility is to stop good material being butchered, not to permit padding. Over 10 is cut. Where a script is long because one beat needs more room, lift that beat into its own video.
- 2026-08-02 — [affects: Torch, Dan] **Domain expertise means the language, not a credentials list.** Aji's definition: "when we use words or explain context in data management, our expertise in that area has to show." Terms used precisely and in the sense a practitioner would recognise; context explained in real operational terms with the failure mode named; the reasoning behind a definition, not just the definition. Never loosen a term's meaning to make a sentence flow. Full rule in `CLAUDE.md`, and it is a pass/fail item on Dan's checklist.
- 2026-08-02 — [affects: Linda] **A beat cut for runtime becomes a proposed video, not a deletion.** The AI-trustworthiness passage was cut from the data management script and added to `backlog.md` as #23, proposed and awaiting approval.
- 2026-08-02 — [affects: Linda, Torch, Dan] **Constitution changes (standing rules) set by Aji:** the hook is the most important element and is protected when cutting; scripts must flow as natural complete sentences with **no stacked fragments**; when over length, **strip whole non-essential sentences, never trim words out of important ones**; **creativity is a requirement**, a correct-but-flat script fails; scripts must read as a **domain expert**, not a generalist. All now in `CLAUDE.md`, plus Dan's checklist and Torch's brief.
- 2026-08-02 — [affects: Torch, Dan] **A rule can be promoted from an agent's observation.** Dan flagged fragment-stacking in his v1 assessment; Aji independently confirmed it; it is now a permanent constitution rule. When Dan flags the same class of issue twice, raise it to Aji as a candidate rule rather than repeating the note.
- 2026-08-02 — [affects: Torch, Dan] **Rich writing runs long.** The v3 rewrite came in at 9.9 minutes before cutting. Torch should draft with the word budget in view from the start; Dan should expect the real trade-off to be *which whole beat leaves*, and put that choice to Aji with a recommendation rather than silently compressing.
- 2026-08-02 — [affects: Linda, Torch, Dan] **Anything agreed with Aji in conversation must be written into memory the same session.** The 7–8 minute runtime had been agreed with her previously but was never recorded, so a full script was written to the old 15-minute rule and had to be rebuilt. If an agreement changes the rules rather than one video, say so and get it into the constitution; if it changes preference or craft, put it in memory. Never let an agreement live only in a chat.
- 2026-08-02 — [affects: Linda, Torch, Dan] **Constitution changes agreed 2026-08-02:** main videos run 7–8 minutes; every video is standalone and topic-first (no book/framework framing); explainers answer what it is, why we need it, how we do it, and nothing else. These are in `CLAUDE.md` and `templates/flagship.md`.
- 2026-08-02 — [affects: Torch] **Currency and figures must match across title, thumbnail and script**, and the channel is UK-facing. Dan has now caught this on two consecutive drafts.
- 2026-08-02 — [affects: Torch] On-camera stats must carry a visible source and stay consistent across title/thumbnail/body (Dan's flag from the "What Is Data Management?" run). Torch: cite inline and align the numbers.
- 2026-08-02 — [affects: Linda, Torch] The AI-era re-hook (bad data → untrustworthy AI) strengthens evergreen foundations topics; Linda briefs it, Torch places it mid-video as a re-hook, not the whole frame.
